
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.9
  creationTimestamp: null
  name: interfaceinstances.core.vedette.io
spec:
  group: core.vedette.io
  names:
    categories:
    - vedette
    kind: InterfaceInstance
    listKind: InterfaceInstanceList
    plural: interfaceinstances
    shortNames:
    - ii
    singular: interfaceinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.claim.name
      name: Claim
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InterfaceInstance is the Schema for the interfaceinstances API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InterfaceInstanceSpec defines the desired state of InterfaceInstance.
            properties:
              capabilities:
                description: Capabilities that this instance support.
                items:
                  type: string
                minItems: 1
                type: array
              capacity:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Required capacity of a suitable InterfaceInstance.
                type: object
              claim:
                description: Back-Reference to the Claim binding to this instance.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              outputs:
                description: Outputs is a list of Secrets or ConfigMaps that holds
                  configuration and credentials to interact with this instance.
                items:
                  properties:
                    configMapRef:
                      description: ConfigMapRef references a ConfigMap in this namespace.
                      properties:
                        name:
                          type: string
                      required:
                      - name
                      type: object
                    name:
                      description: Name of this output reference.
                      type: string
                    secretRef:
                      description: SecretRef references a Secret in this namespace.
                      properties:
                        name:
                          type: string
                      required:
                      - name
                      type: object
                  required:
                  - name
                  type: object
                type: array
              provisionerName:
                description: "Name of the provisioner this instance was assigned to.\t//"
                type: string
              reclaimPolicy:
                default: Delete
                description: What happens with this Instance after it is released
                  from a Claim.
                type: string
            required:
            - capabilities
            type: object
          status:
            description: InterfaceInstanceStatus defines the observed state of InterfaceInstance.
            properties:
              conditions:
                description: Current conditions that apply to this instance.
                items:
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transits from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is the human readable message indicating
                        details about last transition.
                      type: string
                    reason:
                      description: Reason is the (brief) reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition, one of ('True',
                        'False', 'Unknown').
                      type: string
                    type:
                      description: Type is the type of the InterfaceInstance condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              outputs:
                items:
                  properties:
                    checksum:
                      format: byte
                      type: string
                    name:
                      description: Name of this output reference.
                      type: string
                  required:
                  - checksum
                  - name
                  type: object
                type: array
              phase:
                description: DEPRECATED. Phase represents the current lifecycle state
                  of this object. Consider this field DEPRECATED, it will be removed
                  as soon as there is a mechanism to map conditions to strings when
                  printing the property. This is only for display purpose, for everything
                  else use conditions.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
